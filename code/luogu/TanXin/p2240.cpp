#include <bits/stdc++.h>
#define d double
using namespace std;
int n, t;

int Sortvalue(d *a)
{
    int len = n;
    for (int i = 0; i < n; i++)
    {
        for (int j = 1; j < n; j++)
        {
            if (a[i][0] < a[j][0])
            {
            }
        }
    }
}
